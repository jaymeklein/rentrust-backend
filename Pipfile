[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = "*"
alembic = "*"
ruff = "*"

[dev-packages]

[requires]
python_version = "3.13"

[scripts]
dev = "fastapi dev api/main.py --reload"
backend = "fastapi run api/main.py --port 9013"
tests = "pytest -vv"
code-check = "ruff check --select I --fix api"
code-format = "ruff format ."
